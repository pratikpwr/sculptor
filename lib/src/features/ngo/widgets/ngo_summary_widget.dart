import 'package:flutter/material.dart';



class NGOSummaryWidget extends StatelessWidget {
  const NGOSummaryWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('NGO Summary'),
    );
  }
}